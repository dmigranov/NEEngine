#include "pch.h"
#include "ActionSystem.h"

/*ActionSystem::ActionSystem(const std::list<ComponentType>& types, std::function<void(Entity*, double)> func) : System()
{
	for(auto type : types)
		SubscribeToComponentType(type);
	m_executeFunc = func;
}*/


